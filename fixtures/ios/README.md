# iOS fixture plan

The initial repository includes source-oriented iOS evaluation manifests. A complete Xcode fixture should be added when macOS CI and simulator runtime evaluation are introduced. Seeded cases should cover Dynamic Type, VoiceOver naming, semantic controls, Reduce Motion, Keychain storage, WebViews, URL handling, and privacy manifests.
