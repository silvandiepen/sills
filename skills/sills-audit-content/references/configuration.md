# Optional project configuration

Look for a project-specific content configuration before inferring standards. It may define audience, purpose, voice, preferred terminology, forbidden terms, reading level, claims, naming rules, and locales. Validate against `schemas/content-config.schema.json` when the schema is available.
