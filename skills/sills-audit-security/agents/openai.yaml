interface:
  display_name: "Security Audit"
  short_description: "Application, repository, dependency, CI/CD, and supply-chain security audit."
  default_prompt: "Use $sills-audit-security to audit this project and write a complete dated report without modifying the product."
policy:
  allow_implicit_invocation: true
