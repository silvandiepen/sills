# Web and API coverage

Review authentication, sessions, authorization, tenant isolation, input validation, output encoding, injection, XSS, CSRF, SSRF, redirects, deserialization, uploads, path traversal, cookies, CORS, CSP, security headers, rate limits, recovery flows, cryptography, transport, sensitive data, API design, errors, debug surfaces, and business logic. Map relevant findings to OWASP ASVS identifiers when verified.
