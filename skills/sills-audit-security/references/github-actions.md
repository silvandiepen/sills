# GitHub Actions

Review least privilege, `pull_request_target`, untrusted checkout and script execution, mutable action tags, token exposure, environment protections, artifact trust, cache poisoning, release permissions, npm token access, and fork behaviour. Pull-request workflows must never receive publishing secrets.
