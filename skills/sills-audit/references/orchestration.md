# Orchestration

Specialists own their findings. The parent owns scope, shared evidence, deduplication, cross-cutting causes, release blockers, and the combined report. Reuse the same browser session, project inventory, authenticated roles, screenshots, traces, logs, and source mappings whenever possible.
