# Web coverage

Review semantics, landmarks, headings, accessible names, keyboard operation, focus order, focus visibility, traps, restoration, forms, validation, status messages, contrast, non-text contrast, zoom, reflow, target size, dragging alternatives, motion, media, timing, authentication, tables, language, and responsive states.

Use Axe or equivalent only as one evidence source. Inspect the rendered accessibility tree where tools permit.
