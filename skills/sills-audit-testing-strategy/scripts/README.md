# Scripts

This directory is reserved for deterministic, self-contained helpers added when the methodology benefits from repeatable collection or validation. Audit scripts must write only inside the selected audit directory.
