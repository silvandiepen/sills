# Scripts

This skill currently relies on existing repository tools and safe, isolated inspection. Future deterministic helpers may validate documentation links, command references, instruction scope, and authority maps, but they must remain report-only.
