# Components and modules

Review responsibility count, domain logic placement, excessive fragmentation, wrapper components, prop forwarding, nesting, circular dependencies, hidden coupling, global state, unnecessary stored derived state, side effects, repeated UI, missing primitives, and framework-domain mixing. File length is only a signal; explain the actual maintainability impact.
