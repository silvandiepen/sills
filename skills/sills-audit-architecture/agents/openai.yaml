interface:
  display_name: "Codebase Architecture Audit"
  short_description: "Repository and application architecture audit."
  default_prompt: "Use $sills-audit-architecture to audit this project and write a complete dated report without modifying the product."
policy:
  allow_implicit_invocation: true
